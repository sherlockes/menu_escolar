// sw.js - Service Worker mínimo para cumplir los requisitos de instalación
self.addEventListener('fetch', function(event) {
    // Este manejador vacío es suficiente para que el navegador permita la instalación
});
